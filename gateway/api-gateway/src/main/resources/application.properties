spring.application.name=api-gateway
server.port=8080

# URLs dos serviços internos (roteamento por prefixo de caminho)
catalog.url=${CATALOG_URL:http://localhost:8081}
inventory.url=${INVENTORY_URL:http://localhost:8082}
budgeting.url=${BUDGETING_URL:http://localhost:8083}

# Origem permitida no CORS (frontend Next.js)
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
