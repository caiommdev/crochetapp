apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: crochetapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      initContainers:
        - name: wait-for-dependencies
          image: busybox:1.36
          command:
            - sh
            - -c
            - >-
              until nc -z catalog-db 5432 && nc -z rabbitmq 5672 && nc -z redis 6379 && nc -z discovery-server 8761;
              do echo "aguardando catalog-db, rabbitmq, redis e discovery-server"; sleep 2; done
      containers:
        - name: catalog-service
          image: catalog-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://catalog-db:5432/catalog_db
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: crochetapp-secrets
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: crochetapp-secrets
                  key: POSTGRES_PASSWORD
            - name: EUREKA_URL
              value: http://discovery-server:8761/eureka/
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: RABBITMQ_PORT
              value: "5672"
            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: crochetapp-secrets
                  key: RABBITMQ_USERNAME
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: crochetapp-secrets
                  key: RABBITMQ_PASSWORD
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: crochetapp
spec:
  selector:
    app: catalog-service
  ports:
    - port: 8081
      targetPort: 8081